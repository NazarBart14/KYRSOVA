<Window x:Class="KYRSOVA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KYRSOVA"
        mc:Ignorable="d"
        Title="Steam Game Parser" Height="470" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="380"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--<Label Grid.Row="0" Grid.Column="0" Content="Genre:" Margin="5"/>-->
        <!--<ComboBox Height="45" Width="100" Grid.Row="1" Grid.Column="0" x:Name="GenreComboBox" Margin="270,5,10,5"
                  ItemsSource="{Binding Genres}" SelectedItem="{Binding SelectedGenre, UpdateSourceTrigger=PropertyChanged}"
                  DisplayMemberPath="." SelectedValuePath="." />-->
        <Button Height="45" Width="100" Grid.Row="1" Grid.Column="1" x:Name="ParseButton" Content="Parse" Margin="110,5,40,5" Click="ParseButton_Click"/>
        <ListBox Grid.Row="0" Grid.Column="1" x:Name="GamesListBox" Margin="5"
                 ItemsSource="{Binding Games}" DisplayMemberPath="Name" SelectedValuePath="."
                 SelectionMode="Extended" SelectionChanged="GamesListBox_SelectionChanged"/>
        <ListBox Grid.Row="0" Grid.Column="0" Margin="5" x:Name="SelectedGamesListBox"/>
        <Button Height="45" Width="100" Grid.Row="1" Grid.Column="1" x:Name="MoveButton" Content="Move" Margin="10,5,160,5" Click="MoveButton_Click"/>
    </Grid>
</Window>